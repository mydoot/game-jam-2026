extends CampaignLevel

## Backward-compatible level script retained for old editor references. The full
## implementation now lives in CampaignLevel so every room uses the same state
## machine, native projectiles, progression, and failure rules.
